spring:
    application:
      name: ivw-eitan-med
    datasource:
        url: jdbc:postgresql://localhost:5432/for-eitan-med
        # url: ${POSTGRES_DATA_SOURCE_URL}
        username: postgres
        password: yeah_right

    jpa:
        hibernate:
            ddl-auto: create

        show-sql: true
        properties:
            hibernate:
                format_sql: true
        # database-platform: org.hibernate.spatial.dialect.postgis.PostgisDialect


